export function $main() : i32 {
@start
    %i = copy 0 : i32
    jmp @loop_cond
@loop_cond
    %cond = slt %i, 10 : i32
    jnz %cond, @loop_body, @loop_end
@loop_body
    %i = add %i, 1 : i32
    jmp @loop_cond
@loop_end
    ret %i : i32
}
