export function $main() : i32 {
@start
    %a = copy 10 : i32
    %b = copy 20 : i32
    jnz 1, @then, @else
@then
    %c = add %a, %b : i32
    jmp @end
@else
    %d = mul %a, %b : i32
    jmp @end
@end
    %e = phi @then %c, @else %d : i32
    ret %e : i32
}
