function $test_forward_ref(%base : i64, %val : i32) : i32 {
@entry
    # Store val into computed ptr which is forward referenced (defined after use)
    store %val, %ptr : i32
    %ptr = add %base, 8 : i64
    ret 0 : i32
}
